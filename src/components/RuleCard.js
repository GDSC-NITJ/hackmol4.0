import React from 'react'

function RuleCard({title, description}) {
  return (
    <div className='' >
        <h1>{title}</h1>
        <div>{description}</div>
    </div>
  )
}

export default RuleCard